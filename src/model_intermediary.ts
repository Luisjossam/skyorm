import ModelBase from "./model_base";

/**
 * `ModelIntermediary` class.
 *
 * This class extends the `ModelBase` class without modifications, serving as a
 * direct copy of the base class for use. It acts as an "intermediate model"
 * that can be used in contexts where a specific model is expected or can be
 * further extended with additional properties or methods if needed.
 *
 * @extends ModelBase
 */
class ModelIntermediary extends ModelBase {}
export default ModelIntermediary;
